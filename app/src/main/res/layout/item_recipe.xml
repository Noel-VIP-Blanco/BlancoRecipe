<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="16dp">

    <!-- TextView to display the cooking step -->
    <TextView
        android:id="@+id/textViewRecipeName"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="20sp"
        android:textStyle="bold"
        android:layout_centerVertical="true"
        android:layout_toStartOf="@+id/recipeMenuImageButton"
        android:layout_alignParentStart="true"/>

    <!-- ImageButton positioned to the right of the TextView -->
    <ImageButton
        android:id="@+id/recipeMenuImageButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/kebab_menu"
        android:backgroundTint="@color/transparentColor"
        android:layout_alignParentEnd="true"
        android:layout_marginStart="8dp"
        android:importantForAccessibility="no" />

</RelativeLayout>
